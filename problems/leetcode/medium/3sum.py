class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        pass 
        def combinations(arr):
            
        # get all set of all combinations len(nums) choose 3
        # double iteration find which ones add up to each other



test = Solution()

assert test.threeSum([-1, 0, 1, 2, -1, -4]) == [[-1, -1, 2], [-1, 0, 1]]